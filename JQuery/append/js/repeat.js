// вставить <h1> після кожного <p> , якщо декілька то декілька?
$('p').after( $('h1') );

// можна вказати конкретно після якого елементу

// $('p').eq(0).after($('h1')); // тут тільки після першого елементу